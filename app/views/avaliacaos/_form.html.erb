<%= form_with(model: avaliacao) do |form| %>
  <% if avaliacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(avaliacao.errors.count, "error") %> prohibited this avaliacao from being saved:</h2>

      <ul>
        <% avaliacao.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pontos %>
    <%= form.text_field :pontos %>
  </div>

  <div class="field">
    <%= form.label :observacoes %>
    <%= form.text_field :observacoes %>
  </div>

  <div class="field">
    <label name="aluno_id">Nome do Aluno:</label>
    <%= form.collection_select(:aluno_id, Aluno.all, :id, :nome,{:selected => @avaliacao.aluno_id}, :required => true)%>
  </div>

  <div class="field">
    <label name="atividade_id">Título:</label>
    <%= form.collection_select(:atividade_id, Atividade.all, :id, :titulo,{:selected => @avaliacao.atividade_id}, :required => true)%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
